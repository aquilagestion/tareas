# APK locales GREFA Tareas

Salida del build local (sin EAS cloud):

```text
GREFA_TAREAS_<version>.apk
last_apk_path.txt
```

Generar:

```powershell
cd C:\grefa-tareas
npm run build:apk
```

O: `build-apk.bat`
